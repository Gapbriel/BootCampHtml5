$(document).ready(function() {    executeCode()    saveCode();    dragDrop(); })function executeCode(){ document.querySelector(".exe-ta").addEventListener('click', function () {      var head = document.querySelector("head");    var newScript = document.createElement("script");    newScript.type = "text/javascript";    newScript.innerHTML = document.querySelector(".txtArea").value;    head.appendChild(newScript);}, false);} function saveCode(){         var area = document.querySelector('.txtArea');                // place content from previous edit                if (!area.value) {                  area.value = window.localStorage.getItem('value');                }                updateLog(false);                  // your content will be saved locally                document.querySelector('.save-ta').addEventListener('click', function () {                  window.localStorage.setItem('value', area.value);                  window.localStorage.setItem('timestamp', (new Date()).getTime());                  updateLog(true);                }, false);                function updateLog(new_save) {                  var log = document.querySelector(".ta-log");                  var delta = 0;                  if (window.localStorage.getItem('value')) {                    delta = ((new Date()).getTime() - (new Date()).setTime(window.localStorage.getItem('timestamp'))) / 1000;                    if (new_save) {                      log.textContent = 'Saved. Content will be available after browser refresh/reopen.';                      setTimeout(function() {                        log.textContent = '';                      }, 3000);                    } else {                      log.textContent = 'last saved: ' + delta + 's ago';                    }                  }                } }function dragDrop(){     var content = document.querySelector('.content');            if (!window.FileReader) {        content.innerHTML = "<p>This browser doesnt support the File API</p>";              // Prints out file properties.        function displayFile(file) {          document.querySelector('.name').textContent = file.fileName;          document.querySelector('.size').textContent = file.fileSize;          document.querySelector('.file-content').style.border = "1px solid black";                var reader = new FileReader();                reader.onload = function(event) {            document.querySelector('.file-content').textContent =              event.target.result;          };                reader.onerror = function() {            document.querySelector('.file-content').innerHTML = 'Unable to read ' + file.fileName;          };                reader.readAsText(file);        }              // Input handler        document.querySelector('.file').onchange = function() {          displayFile(this.files[0]);        };              // Add invisible border to drop area        content.style.border = '4px solid transparent';              // Add dragging events        content.ondragenter = function() {          content.style.border = '4px solid #b1ecb3';          return false;        };              content.ondragover = function() {          return false;        };              content.ondragleave = function() {          return false;        };              content.ondrop = function(event) {          content.style.border = '4px solid transparent';          displayFile(event.dataTransfer.files[0]);          return false;        };      }}